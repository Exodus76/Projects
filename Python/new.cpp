#include<iostream>
using namespace std;
class abc{
    int len;
    char str;
    public:
    abc();
}
abc(char *ptr)
{
    len = strlen(ptr);
    {
        un = strun(ptr);
        str = new char[un];
        strcpy(str,ptr);
    }void getdata()
    cout<<"Enter the string" << endl;
    cin>> str;
}void putdata()
}cout << end << str;
}